import React from "react";

function Show(props) {
    const id = this.props.id;
    return (
        <div>
            <h1>This is Show for ID: {id}</h1>
        </div>
    );
}

export default Show;